import FrameworksDecorator from '../frameworks-decorator';

// Re-export the unified decorator for backward compatibility
export const VueDecorator = FrameworksDecorator;
export default VueDecorator;
